const fs = require('fs');
const path = require('path');

// Create a simple PNG icon using a different approach
// This creates a minimal PNG file that should work for the menu bar

function createSimpleIcon() {
  // This is a minimal 16x16 PNG file with a simple icon
  // The data represents a simple black and white icon
  const pngData = Buffer.from([
    0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A, 0x00, 0x00, 0x00, 0x0D,
    0x49, 0x48, 0x44, 0x52, 0x00, 0x00, 0x00, 0x10, 0x00, 0x00, 0x00, 0x10,
    0x08, 0x02, 0x00, 0x00, 0x00, 0x90, 0x91, 0x68, 0x36, 0x00, 0x00, 0x00,
    0x0C, 0x49, 0x44, 0x41, 0x54, 0x78, 0x9C, 0x63, 0x60, 0x18, 0x05, 0x03,
    0x00, 0x00, 0x30, 0x00, 0x00, 0x01, 0x5D, 0x5D, 0xB6, 0x5A, 0x00, 0x00,
    0x00, 0x00, 0x49, 0x45, 0x4E, 0x44, 0xAE, 0x42, 0x60, 0x82
  ]);
  
  const outputPath = 'assets/icon.png';
  
  // Ensure assets directory exists
  const assetsDir = path.dirname(outputPath);
  if (!fs.existsSync(assetsDir)) {
    fs.mkdirSync(assetsDir, { recursive: true });
  }
  
  // Write the PNG file
  fs.writeFileSync(outputPath, pngData);
  console.log('✅ Simple icon created at:', outputPath);
  console.log('This is a minimal 16x16 PNG icon that should work in the menu bar.');
}

createSimpleIcon(); 